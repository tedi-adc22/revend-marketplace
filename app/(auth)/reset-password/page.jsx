import ResetPasswordForm from "@/components/AuthComponents/ResetPasswordForm";

export const metadata = {
  title: "Reset Password | Revend",
};

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}
